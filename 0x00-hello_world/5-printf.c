#include <stdio.h>

/**
 * main - this is the main that piece art
 *
 * Return: 0 when it completes success
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
